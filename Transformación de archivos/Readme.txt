Para ejecutar el instalador debe instalar un ambiente con python 3.10 y luego instalar pyinstaller y compilar el archivo con el comando:

pyinstaller --onefile --noconsole form.py

NOTA: no se incluye el instaldor debido a que es muy pesado y github no permite subirlo.
